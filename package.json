{
  "name": "test-trello-clone",
  "version": "1.0.0",
  "description": "test build of a trello clone",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db/db.json"
  },
  "repository": {
  "type": "git",
  "url": "https://github.com/nicoraven/trello-like.git"
  },
  "author": "nicoraven",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.15.0"
  }
}